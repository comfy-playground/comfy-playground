# Example Inputs

These are sanitized configuration inputs for the deployment guides. They are
not complete production Compose projects and intentionally contain no model
mount paths, GPUs, LAN addresses, credentials or image tags.

- `single-node/`: direct worker address convention.
- `single-node-mcp/`: MCP environment for direct worker mode.
- `cluster/`: Gateway worker list and MCP environment for clustered mode.
